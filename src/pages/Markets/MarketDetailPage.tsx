import { StackView } from "~/components";
import { PageLayout } from "../_containers";

export const MarketDetailPage = () => {
  return (
    <PageLayout>
      <StackView flex />
    </PageLayout>
  );
};
